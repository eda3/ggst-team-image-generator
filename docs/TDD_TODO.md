# テスト駆動開発 (TDD) ToDoリスト

このドキュメントは、GGST 3on3チーム画像生成ツールのテスト駆動開発におけるテストToDoリストです。

## 凡例

- [ ] 未着手
- [x] 完了
- 🔴 優先度高（基本機能）
- 🟡 優先度中（拡張機能）
- 🟢 優先度低（Phase 2以降）

---

## 1. packages/core テスト

### 1.1 CharacterRegistry（キャラクター管理）

**ファイル**: `tests/core/CharacterRegistry.test.ts`

#### 基本機能 🔴
- [x] IDでキャラクターを取得できる
- [x] 日本語名でキャラクターを取得できる
- [x] エイリアスでキャラクターを取得できる
- [x] 大文字小文字を区別しない
- [x] 存在しないキャラクターはundefinedを返す
- [x] 空文字列はundefinedを返す
- [x] 前後の空白を無視する

#### 一覧取得 🔴
- [x] 全キャラクターを取得できる
- [x] 全キャラクターにid、name、nameJaがある
- [x] 全キャラクターIDを取得できる

#### 存在確認 🔴
- [x] hasCharacter: 存在するキャラクターはtrueを返す
- [x] hasCharacter: 存在しないキャラクターはfalseを返す

#### インスタンス 🟡
- [x] 新しいインスタンスも同じデータを持つ

---

### 1.2 TeamBuilder（チーム構築）

**ファイル**: `tests/core/TeamBuilder.test.ts`

#### 基本的なチーム構築 🔴
- [x] 3人のメンバーでチームを構築できる
- [x] エイリアスでキャラクターを指定できる
- [x] 同じキャラクターを複数人が使用できる（重複許可）

#### parseMembersString 🔴
- [x] 文字列からメンバーをパースできる
- [x] 空白を含む文字列をパースできる
- [x] 日本語のエイリアスを使用できる

#### エラーケース 🔴
- [x] チーム名なしでビルドするとエラー
- [x] 空のチーム名はエラー
- [x] 3人未満でビルドするとエラー
- [x] 4人目を追加しようとするとエラー
- [x] 存在しないキャラクターを指定するとエラー
- [x] 空のプレイヤー名はエラー
- [x] 不正なメンバー形式はエラー

#### reset機能 🔴
- [x] resetでビルダーの状態がクリアされる

#### メソッドチェーン 🔴
- [x] 全てのメソッドがthisを返す

---

### 1.3 ImageRenderer（画像レンダリング）

**ファイル**: `tests/core/ImageRenderer.test.ts`

#### 基本的な画像生成 🔴
- [x] horizontalレイアウトで画像を生成できる
- [x] verticalレイアウトで画像を生成できる
- [x] 生成されたバッファがPNG形式である（マジックナンバー確認）
- [x] 生成されたバッファのサイズが0より大きい

#### カスタムサイズ 🔴
- [x] 指定した幅で画像を生成できる
- [x] 指定した高さで画像を生成できる
- [x] デフォルトサイズ（800x300）で生成できる
- [x] 幅と高さを同時に指定できる

#### カスタムスタイル 🟡
- [x] 背景色を指定できる
- [x] フォントサイズを指定できる
- [x] フォントカラーを指定できる
- [x] 複数のスタイルオプションを同時に指定できる

#### エラーケース 🔴
- [x] キャラクター画像が見つからない場合、プレースホルダーを表示する
- [x] 空のメンバーリストでも画像が生成される
- [x] 長いチーム名でも画像が生成される
- [x] 長いプレイヤー名でも画像が生成される

#### RenderResult 🔴
- [x] RenderResultにbuffer, width, heightが含まれる
- [x] widthとheightが指定値と一致する

---

### 1.4 RenderOptions（レンダリングオプション）

**ファイル**: `tests/core/ImageRenderer.test.ts`（ImageRendererテストに統合）

#### デフォルト値 🔴
- [x] DEFAULT_RENDER_OPTIONSが正しいデフォルト値を持つ
- [x] layoutのデフォルトは"horizontal"
- [x] widthのデフォルトは800
- [x] heightのデフォルトは300
- [x] backgroundColorのデフォルトは"#1a1a2e"
- [x] fontColorのデフォルトは"#ffffff"
- [x] fontSizeのデフォルトは24
- [x] fontFamilyのデフォルトは"Noto Sans JP"

---

## 2. packages/cli テスト

### 2.1 parseArgs（引数パーサー）

**ファイル**: `tests/cli/parser.test.ts`

#### コマンドのパース 🔴
- [x] generateコマンドをパースできる
- [x] listコマンドをパースできる
- [x] コマンドがない場合は空文字

#### フラグのパース 🔴
- [x] -h でhelpがtrueになる
- [x] --help でhelpがtrueになる
- [x] -v でversionがtrueになる
- [x] --version でversionがtrueになる

#### オプションのパース 🔴
- [x] -t でチーム名を指定できる
- [x] --team でチーム名を指定できる
- [x] -m でメンバー情報を指定できる
- [x] --members でメンバー情報を指定できる
- [x] -o で出力ディレクトリを指定できる
- [x] -l でレイアウトを指定できる (horizontal)
- [x] -l でレイアウトを指定できる (vertical)
- [x] --width で幅を指定できる
- [x] --height で高さを指定できる

#### 複数オプションの組み合わせ 🔴
- [x] 全オプションを組み合わせてパースできる

#### エラーケース 🔴
- [x] -t の後に値がないとエラー
- [x] -t の後に別のオプションがあるとエラー
- [x] -m の後に値がないとエラー
- [x] -o の後に値がないとエラー
- [x] -l の後に値がないとエラー
- [x] -l に不正な値を指定するとエラー
- [x] --width の後に値がないとエラー
- [x] --width に数値以外を指定するとエラー
- [x] --width に0を指定するとエラー
- [x] --width に負の数を指定するとエラー
- [x] --height に数値以外を指定するとエラー

#### 境界ケース 🔴
- [x] 空の引数配列
- [x] 不明なオプションは無視される
- [x] 日本語のチーム名を指定できる
- [x] スペースを含むチーム名を指定できる

---

### 2.2 generateコマンド

**ファイル**: `tests/cli/generate.test.ts`

#### 正常系 🔴
- [x] 有効な引数で画像ファイルが生成される
- [x] 出力ディレクトリが存在しない場合は自動作成される
- [x] 生成されたファイルがPNG形式である

#### 出力確認 🔴
- [x] 成功時にファイルパスが出力される
- [x] 成功時に画像サイズが出力される

#### エラー系 🔴
- [x] チーム名がない場合にエラーメッセージが表示される
- [x] メンバー情報がない場合にエラーメッセージが表示される
- [x] 無効なキャラクターIDでエラーメッセージが表示される
- [x] exit code 1で終了する

---

### 2.3 listコマンド

**ファイル**: `tests/cli/list.test.ts`

#### 正常系 🔴
- [ ] 全キャラクターが表示される
- [ ] キャラクターIDが表示される
- [ ] キャラクター名（日本語）が表示される
- [ ] エイリアスが表示される
- [ ] 合計数が表示される

---

### 2.4 CLI統合テスト

**ファイル**: `tests/cli/integration.test.ts`

#### E2Eテスト 🔴
- [ ] `bun run dev generate` で画像生成が成功する
- [ ] `bun run dev list` でキャラクター一覧が表示される
- [ ] `bun run dev --help` でヘルプが表示される
- [ ] `bun run dev --version` でバージョンが表示される

#### エラーハンドリング 🔴
- [ ] 不正な引数でエラー終了する（exit code 1）
- [ ] ParseErrorが適切にハンドリングされる

---

## 3. Phase 2: 拡張機能テスト 🟢

### 3.1 batchコマンド

**ファイル**: `tests/cli/batch.test.ts`

#### JSON入力 🟢
- [ ] JSONファイルから複数チームを読み込める
- [ ] 各チームの画像が生成される

#### YAML入力 🟢
- [ ] YAMLファイルから複数チームを読み込める

#### エラーケース 🟢
- [ ] 無効なJSONファイルでエラー
- [ ] ファイルが存在しない場合にエラー

#### パフォーマンス 🟢
- [ ] 10チームの一括生成が3秒以内に完了する

---

### 3.2 テンプレートシステム

**ファイル**: `tests/core/TemplateEngine.test.ts`

#### テンプレート読み込み 🟢
- [ ] デフォルトテンプレートを読み込める
- [ ] カスタムテンプレートを読み込める
- [ ] 存在しないテンプレートでエラー

#### テンプレート適用 🟢
- [ ] テンプレートのスタイルが適用される

---

### 3.3 interactiveコマンド

**ファイル**: `tests/cli/interactive.test.ts`

#### 対話入力 🟢
- [ ] チーム名の入力を受け付ける
- [ ] プレイヤー名の入力を受け付ける
- [ ] キャラクター検索が動作する
- [ ] レイアウト選択が動作する

---

## 4. Phase 4: 対戦カード生成テスト 🟢

### 4.1 MatchCardRenderer

**ファイル**: `tests/core/MatchCardRenderer.test.ts`

#### 対戦カード生成 🟢
- [ ] 2チームを左右に配置できる
- [ ] VS表示が含まれる
- [ ] 大会名を追加できる
- [ ] ラウンド情報を追加できる

---

## 5. ユーティリティテスト

### 5.1 バリデーション

**ファイル**: `tests/core/validation.test.ts`

#### チーム名バリデーション 🟡
- [ ] 空文字列は無効
- [ ] 空白のみは無効
- [ ] 最大文字数制限（あれば）

#### プレイヤー名バリデーション 🟡
- [ ] 空文字列は無効
- [ ] 空白のみは無効

#### メンバー数バリデーション 🔴
- [ ] 3人未満は無効
- [ ] 3人を超えると無効

---

## 6. 実行順序（推奨）

### Phase 1: 基本機能（現在）

1. ~~CharacterRegistry テスト~~ ✅ 完了
2. ~~TeamBuilder テスト~~ ✅ 完了
3. ~~parseArgs テスト~~ ✅ 完了
4. ~~ImageRenderer テスト~~ ✅ 完了
5. ~~generateコマンド テスト~~ ✅ 完了
6. **listコマンド テスト** ← 次に実装
7. **CLI統合テスト**

### Phase 2: 拡張機能

8. batchコマンド テスト
9. テンプレートシステム テスト
10. interactiveコマンド テスト

### Phase 4: 対戦カード

11. MatchCardRenderer テスト

---

## 7. テスト実行コマンド

```bash
# 全テスト実行
bun test

# 特定ファイルのみ
bun test tests/core/ImageRenderer.test.ts

# パターンマッチ
bun test --test-name-pattern "ImageRenderer"

# watchモード
bun test --watch

# カバレッジ付き
bun test --coverage

# 詳細出力
bun test --verbose
```

---

## 8. カバレッジ目標

| モジュール | 現在 | 目標 |
|-----------|------|------|
| CharacterRegistry | 100% | 100% |
| TeamBuilder | 100% | 100% |
| parseArgs | 100% | 100% |
| ImageRenderer | 100% | **80%以上** ✅ |
| generateコマンド | 99.5% | 80%以上 ✅ |
| listコマンド | 0% | 80%以上 |
| **全体** | **99.97%** | **90%以上** ✅ |

---

## 9. TDDサイクル

各テスト項目に対して以下のサイクルを実行：

1. **Red**: 失敗するテストを書く
2. **Green**: テストを通す最小限の実装
3. **Refactor**: コードを改善

```typescript
// 例: ImageRenderer のTDDサイクル

// 1. Red - 失敗するテストを書く
test("horizontalレイアウトで画像を生成できる", async () => {
  const renderer = new ImageRenderer();
  const team = createMockTeam();
  const result = await renderer.render(team, { layout: "horizontal" });

  expect(result.buffer).toBeInstanceOf(Buffer);
  expect(result.buffer.length).toBeGreaterThan(0);
});

// 2. Green - テストを通す実装
// 3. Refactor - 改善
```

---

## 更新履歴

| 日付 | 更新内容 |
|------|---------|
| 2026-01-16 | 初版作成 |
| 2026-01-16 | ImageRenderer, RenderOptionsテスト完了（26テスト追加、計90テスト） |
| 2026-01-16 | generateコマンドテスト完了（9テスト追加、計99テスト） |
